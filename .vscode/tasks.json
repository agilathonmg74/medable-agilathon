{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            // "showOutput": "always", --DEPRECATED, see eslint output, reveal instead
            "command": "npm",
            "args": [
                "run", "build"
            ],
            "presentation": {
                "reveal": "always"
            }
        }
    ]
  }